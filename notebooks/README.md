# Notebooks

The original exploratory notebook should not be used as the primary reproducibility artifact because it contained local machine paths and manual edits. Keep only clean demonstration notebooks here.

The reproducible workflow is:

```bash
python experiments/run_single.py --config configs/debug_smoke_test.yaml
```
